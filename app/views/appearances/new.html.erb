<%=  form_for :appearance do |f| %>
<%= f.label :guest %>
<%= f.collection_select :guest_id, Guest.all, :id, :name %>
<%= f.label :episode %>
<%= f.collection_select :episode_id, Episode.all, :id, :number %>
<%= f.label :rating %>
  <%#i would have loved to put in 1-5 in a drop down here ( I know that the way to validate it though is through a validation on the class) -->
<%<!-- #time crunch here, would have put a number feild -->%>
<%= f.submit %>
<%end%>
